# Threads Adapter

Split the source into short plain-text posts. Use `POST 1:` and `---` separators. Keep the opening useful without context, and put a link only where the source explicitly provides one. Validate each post before publishing.
